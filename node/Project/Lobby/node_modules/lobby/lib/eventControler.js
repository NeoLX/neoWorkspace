
var events = require('events');
var util = require('util');

function EventControler(){
    this.addEvent = function(key, func){
        this.on(key, func);
    };

    this.startEvent = function(key){
        this.emit(key);
    };
}
util.inherits(AppBase,events.EventEmitter);